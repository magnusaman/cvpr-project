# PyTorch CPU-only (much smaller ~200MB vs 2GB for CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# YOLOv8 (without bundled PyTorch)
ultralytics>=8.0.0

# Image processing
Pillow>=9.0.0

# Web framework
flask>=2.0.0
flask-cors>=3.0.10
werkzeug>=2.0.0

# Data handling (minimal)
numpy>=1.21.0
